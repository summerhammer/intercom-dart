name: intercom_api
description: "Unofficial Intercom REST API client written in Dart"
version: 0.2.0
license: MIT
homepage: https://github.com/summerhammer/intercom-dart

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  json_annotation: ^4.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.9
  json_serializable: ^6.7.1
